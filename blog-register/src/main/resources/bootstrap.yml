server:
  port: 8761
eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false #不注册本身服务
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
